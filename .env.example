# Optional: raises GitHub API limits and permits access to repositories the token can read.
GITHUB_TOKEN=

# Optional: GitHub Enterprise-compatible REST API root.
GITHUB_API_URL=https://api.github.com
